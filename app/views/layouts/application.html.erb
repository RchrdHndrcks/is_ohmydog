<!DOCTYPE html>
<html>
  <head>
    <title>Oh My Dog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

<p class="home_bar">
Novedades&emsp; Adopciones&emsp; Paseadores/Cuidadores&emsp;

  <% if current_user && !current_user.es_admin %> <!-- Verifica si el usuario existe y no es administrador -->
  <%= link_to "Pedir turno", new_appointment_path, class: "button" %>
  <%= link_to "Mis turnos", appointments_path(@user_id), class: "button" %>
  <% end %>
  <% if current_user && current_user.es_admin %> <!-- Verifica si el usuario existe y es administrador -->
  <%= link_to "Turnos", appointments_path(@user_id), class: "button" %>
  <% end %>

  <% if current_user %> <!-- Verifica si el usuario existe -->
  <%= link_to "Cerrar sesión", logout_path, method: :delete, class: "button" %>
  <% else %>
  <%= link_to "Iniciar sesión", login_path, class: "button" %>
  <%= link_to "Registrarse", signup_path, class: "button" %>
  <% end %> 
</p> 

  <body>
    <%= yield %>
  </body>
</html>
